<div class="container">
	<div class="wrapper">
		<div class="page-header">
			{{#link-to 'disasters' class='logo test-disasters-header'}}
				<img src='/assets/img/logo.svg' />
			{{/link-to}}
		</div>

		<div class="work-area">
			{{outlet}}
		</div>
	</div>
</div>